# 原始資料目錄 (Raw Sources)

這個資料夾用來放置你所收集的一切原始資料，例如：
- 網頁文章擷取 (Markdown 或文字)
- 論文 (PDF 等，若有文字解析版)
- 純文字筆記
- 參考資料

### ⚠️ 重要守則
這個資料夾內的檔案代表「絕對的真實來源 (Source of Truth)」。
**這些檔案對 LLM 來說都是唯讀的 (Immutable)**，LLM 只負責過來這裡閱讀跟擷取資訊，但絕對不能覆寫或刪除這裡的檔案。

如果來源內包含圖片或附件檔，請放置於 `raw/assets/` 資料夾內。
