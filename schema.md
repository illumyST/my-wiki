# LLM Wiki Schema

## 系統定位 (The core idea)
這是一個由 LLM 維持與管理的個人知識庫。與一般的 RAG 系統不同，LLM 並不僅是在查詢時檢索文件，而是「增量地建立和維護一個持久的 Wiki」—— 一個結構化、互相連結的 Markdown 檔案集合。
你（使用者）負責探索、提供資料來源並提出好問題；我（LLM）將負責繁雜的工作：摘要、建立交叉參照、歸檔及維持系統的一致性。

## 架構說明 (Architecture)
整個知識庫分為三個不同的層級與資料夾結構：

1. **Raw (原始來源)** - 位於 `raw/` 目錄
   這些是你收集的原始文件（文章、論文、圖片、資料檔等）。
   - **規則**：對 LLM 而言，這些檔案是「唯讀的 (Immutable)」。LLM 會從這裡讀取知識，但絕不能修改任何原始檔案，這裡代表了絕對的真實來源 (Source of Truth)。

2. **Wiki (知識庫本部)** - 位於 `wiki/` 目錄
   這是 LLM 完全擁有的資料夾，存放各式 Markdown 檔案（如摘要、實體頁面、概念頁面、分析比較）。
   - **規則**：LLM 負責創建、更新並維護這些頁面的交叉連結。當新資料進來時，LLM 必須更新這些舊有頁面以維持知識的一致性。

3. **Schema (架構約定)** - 也就是本檔案 `schema.md`
   這個檔案告訴 LLM 該如何運作這個 Wiki，約定好了處理資料、回答問題或維護知識庫的標準流程。隨著時間推移，使用者可以和 LLM 一起演進這份檔案。

## 核心操作流程 (Operations)

### 1. Ingest (攝取指令)
當使用者在 `raw/` 放入新資料，並指示 LLM 進行攝取時，LLM 應：
1. 讀取該原始來源。
2. 和使用者討論並確認重點。
3. 在 `wiki/` 內建立該來源的總結頁面。
4. 尋找 `wiki/` 內既有的「實體」與「概念」頁面，並將新知識整合進去（或創建新的相關頁面）。
5. 更新 `index.md`，將新建立或影響的頁面編入目錄。
6. 在 `log.md` 紀錄本次動作。

### 2. Query (查詢指令)
當使用者對知識庫提出問題時：
1. LLM 應優先閱讀 `index.md` 中列出的清單，找出可能相關的頁面。
2. 進入這些頁面閱讀後，融合知識並帶上引用向使用者回答。
3. **重要**：如果該次回答產生了很有價值的總結、比較表或分析，LLM 應主動提議將該回答轉換為新的 Wiki 頁面並存入 `wiki/`，讓探索的足跡得以累積。

### 3. Lint (健康檢查)
當使用者定期要求 Lint 時，LLM 需要：
- 檢查檔案間互相矛盾的資訊，或是已被新文件推翻的舊主張。
- 找出缺乏反向連結的孤兒頁面 (Orphan pages)。
- 找出被多次提及卻沒有專屬實體/概念頁面的關鍵字。
- 提議使用者下一步可以探索或補充哪些未知領域。

## 特殊追蹤檔案
為了讓系統可以在適當規模下良好運作，有兩個核心追蹤檔置於根目錄：
- `index.md`：以內容為導向的完整目錄，條列 Wiki 內所有頁面的連結與單行介紹，供 LLM 查詢時的第一步導航。
- `log.md`：以時間為導向的僅附加 (Append-only) 歷史紀錄檔。記錄每次 Ingest, Query 的時間點與標題。
